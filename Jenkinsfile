pipeline {
    agent any
    environment {
        PACKAGE_NAME = 'sara_msgs'
    }
    stages{
        stage('Setup') {
            steps {
                sh 'printenv'
            }
        }
    }
}
